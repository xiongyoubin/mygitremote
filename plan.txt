1：使用featexp，进行数据预处理（已完成）

2：对数据结构重新理解

3：根据相关性图，将特征与特征，特征与target筛选，根据模型，可视化（特征工程大纲按照git来）

4：异常值筛选（箱型图）

5：用6月份数据，测试模型是否过拟合

6：使用甲方可以理解的评估方式去作为模型评估指标

8：将标签替换为其频率，这代表了值的分布信息，可用于树模型和线性模型
	[S,C,Q] -> [0.5,0.3,0.2]
encoding = titanic.groupby('Embarked').size()
encoding = encoding / len(titanic)
titanic['enc'] = titanic.Embarked.map(encoding)

9：多重共线性的检验

10：变量不显著直接剔除

11：学习4.0文档，以及HMM在网络安全的使用

12：南方基地用独立森林

13：使用woe编码，badgrate，IV值去处理

14：将投诉的数据和非投诉的数据进行对比，进而发现投诉的数据有什么特点

15：参考思路数据分析与机器学习实战